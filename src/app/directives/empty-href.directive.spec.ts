import { EmptyHrefDirective } from './empty-href.directive';

describe('EmptyHrefDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyHrefDirective();
    expect(directive).toBeTruthy();
  });
});
